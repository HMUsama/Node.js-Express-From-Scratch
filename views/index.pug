extends layout

block content
    h1 #{title}
        ul
        each article , i in articles
            li= article.title
            //-   h5= article.auther
            //-     h5= article.body
            